import type {Solver} from "../types/Solver.ts";

export const DancingLinksSolver: Solver = {
    name: "Dancing Links",

    solve(board) {
        return;
    },

    // Schrittmodus optional, DLX ist schwerer zu visualisieren
    solveSteps(board: number[][], steps: number) {
        return;
    }
};