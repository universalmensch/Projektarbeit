import type {Solver} from "../types/Solver.ts";

export const ConstraintSolver: Solver = {
    name: "Constraint Propagation",

    solve(board) {
        return;
    },

    solveSteps(board: number[][], steps: number) {
        return;
    }
};